name: Sync Node Schemas

on:
  push:
    branches:
      - main
    paths:
      - "AI Infra Relationship.md"
      - "company/**/*.md"
      - "community/**/*.md"
      - "university/**/*.md"
      - "schema/*.yaml"
      - "scripts/audit-graph.py"
      - "scripts/audit-typed-relations.py"
      - "scripts/generate-node-schemas.py"
      - "scripts/repair-markdown-quality.py"
      - ".github/workflows/sync-node-schemas.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-node-schemas-main
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Build pre-migration graph inventory
        run: python3 scripts/audit-graph.py --root . --output generated

      - name: Repair Markdown quality
        run: python3 scripts/repair-markdown-quality.py --root . --generated generated

      - name: Rebuild graph inventory after repair
        run: python3 scripts/audit-graph.py --root . --output generated

      - name: Validate typed relations
        run: python3 scripts/audit-typed-relations.py --root . --generated generated

      - name: Generate one schema instance per Markdown node
        run: |
          python3 scripts/generate-node-schemas.py \
            --root . \
            --nodes generated/nodes.json \
            --output schema/nodes \
            --manifest schema/node-manifest.json

      - name: Commit repaired Markdown and synchronized schemas
        shell: bash
        run: |
          if [[ -z "$(git status --porcelain -- 'AI Infra Relationship.md' company community university schema/nodes schema/node-manifest.json)" ]]; then
            echo "Markdown and node schemas already synchronized."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 'AI Infra Relationship.md' company community university schema/nodes schema/node-manifest.json
          git commit -m "chore(graph): repair Markdown quality and sync schemas"
          git push origin HEAD:main
