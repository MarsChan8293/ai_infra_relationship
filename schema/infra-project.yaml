schema: ai-infra-relationship/infra-project-v1
status: deprecated
canonical_type: project
replacement: schema/project.yaml
notes:
  - Do not create new infra-project nodes.
  - Legacy infra-project Markdown must migrate to type: project.
  - The node-schema generator maps legacy infra-project to project during the migration window.
